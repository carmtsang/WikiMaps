
const path = window.location.pathname.split('/')
const mapId = path[2]



// const showPins = pins => {
//   for (let pin of pins) {
//     let $coords = latlong(pin);
//     console.log($coords)
//     $('#test').prepend($coords)
//   }
// }

// const latlong = coord => {
//   return `<p>{ lat: ${coord.longitude}, lng: ${coord.latitude} }</p>`
// };


// const getMapCoords = () => {
//   $.ajax(`/api/all_markers/${mapId}`, { method: 'GET' })
//     .then(pins => showPins(pins))
// }

// getMapCoords()





// const path = window.location.pathname.split('/')
// const mapId = path[2]



// initialzie map onto the page
